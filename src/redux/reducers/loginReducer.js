

const INITITIAL_STATE = {
    user: '',
    loading: false,
}

const loginReducer = (state = INITITIAL_STATE, { type, payload }) => {
    switch (type) {
        default:
            return state;
    }
}